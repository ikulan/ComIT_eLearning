@model ComIT_eLearning.Areas.Admin.ViewModels.CreateTeacherViewModel

@{
    ViewData["Title"] = "Create a Teacher Account";
}

<div class="d-flex justify-content-between align-items-end">
    <h1 class="mb-0">@ViewData["Title"]</h1>
    <p class="mb-0"><a asp-action="Index">Back to List</a></p>
</div>

<hr />

@await Html.PartialAsync("_Form")
